# Claude Code 部署

> 代码：[deploy/claude.py](claude.py)  |  标志：`-c` / `--claude`

## 部署目标

```
{target}/
├── CLAUDE.md                     # 项目级指令
└── .claude/commands/
    ├── rule-compile.md           # /rule-compile 命令
    └── rule-validate.md          # /rule-validate 命令
```
